# Examples

The public dataset is expected at `../data/` and generated products at `../outputs/`, as shown in the repository README. Use `config.example.yaml` and `DATA_MANIFEST.yaml` as the starting configuration. No scientific analysis is executed automatically.
